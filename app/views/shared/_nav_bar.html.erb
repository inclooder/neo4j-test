<div class="nav-bar">
   <% if @current_user %>
      <div class="logout">
         <%= link_to "Logout", session_path, method: :delete %>
      </div>
      <div>
         Logged in as <%= @current_user.name %>
      </div>
   <% end %>
</div>
